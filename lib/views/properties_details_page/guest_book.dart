import 'package:flutter/material.dart';

class GuestBook extends StatelessWidget {
  const GuestBook({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Guest Book'));
  }
}
